# Owner-only access for the standalone cost optimization app.
COST_OWNER_EMAIL=

# Default AWS region for orderability catalog lookups when endpoint inference is unavailable.
AWS_REGION=us-east-1

# Local manual upload server port.
PORT=3001
