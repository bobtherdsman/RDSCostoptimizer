# Fixtures

Add small deterministic fixtures here before implementing optimizer behavior:

- low CPU, memory fits
- low CPU, memory blocked
- low CPU, IOPS blocked
- low CPU, throughput blocked
- DB-level I/O offender